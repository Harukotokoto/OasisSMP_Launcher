appId: 'org.harukoto.launcher'
productName: 'Harukoto Project'
artifactName: '${productName}-setup-${version}.${ext}'

copyright: 'Copyright © 2026 Harukoto'

asar: true
compression: 'maximum'

files:
  - '!{dist,.gitignore,.vscode,docs,dev-app-update.yml,.nvmrc,eslint.config.mjs}'

extraResources:
  - 'libraries'

# Windows Configuration
win: 
  target: 
    - target: 'nsis'
      arch: 'x64'

# Windows Installer Configuration
nsis:
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: true

# macOS Configuration
mac:
  target:
    - target: 'dmg'
      arch:
        - 'x64'
        - 'arm64'
  artifactName: '${productName}-setup-${version}-${arch}.${ext}'
  category: 'public.app-category.games'

# Linux Configuration
linux: 
  target: 'AppImage'
  maintainer: 'Harukoto'
  vendor: 'Harukoto'
  synopsis: 'Oasis SMP Launcher'
  description: 'Oasis SMP 専用のカスタムランチャーです。Mod や設定の管理・更新を自動で行います。'
  category: 'Game'


directories:
  buildResources: 'build'
  output: 'dist'